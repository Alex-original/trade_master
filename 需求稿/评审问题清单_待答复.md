# 「模拟交易+AI托管」App — 需求评审问题清单

> 评审对象：`模拟交易AI托管App_PRD需求文档.docx` V1.0（2026-09-08）／`模拟交易AI托管App_UI设计需求文档.docx` V1.0
> 使用方式：在每题「答：」后直接填写你的决定/倾向即可；每题都给了建议默认值，可直接采纳或修改。

---

## A. 必须先答（阻塞性，决定架构与开发量）

### A1. 与现有 Trade Master 深度分析引擎的关系？
现在 `product/trade_master` 已有一个能跑的「单标的 10–15 分钟深度分析引擎」（Wind + DeepSeek + TradingAgents 多智能体）。
新 App 是把它当底座加托管调度层 + 移动端，还是全新独立、只复用数据/LLM 层？

答：a
- 可选：a) 深度引擎当底座（复用其分析/报告/数据层）
- 可选：b) 全新独立产品，仅复用 Wind + DeepSeek 封装
- 可选：c) 其他 ______

---

### A2. 目标载体？
UI 稿按 iPhone 15 Pro 基准、要唤起相册/粘贴板；正文路径 `/pages/…` 又像小程序/uni-app。V1.0 交付到哪个载体？

答：d（web应用，同之前做的video_to_note项目
- 可选：a) 手机 App（原生 / 跨端）　b) 移动 H5　c) 小程序　d) 其他 ______

- 若做 App：技术栈是否定了？______
- 「第三方快捷登录」V1.0 是否要做？做哪家（微信 / Apple）？______

---

### A3. 「财团托管」的核心策略 spec（目前缺失）
文档只写了「实时扫描行情 → 生成调仓决策」，没有可执行规则。请逐条确认：
完全复用Trade Master 深度分析引擎，你评审一下是否可行

1. 托管 Agent 依据什么信号调仓？（动量 / 基本面 / 止盈止损 / 定期再平衡 / 其他）
   答：托管agent按照Trade Master 深度分析引擎的信号和建议来调仓
2. 调仓触发方式？（定时扫描 / 价格阈值 / 每笔实时判断 / 每日收盘跑一次次日计划）
   答：
3. 标的池（V1.0）=？（仅持仓股 / 持仓+建议池 / 全市场）
   答：
4. 单票仓位上限、单日最大交易次数：是硬性风控还是可选参数？
   答：可选参数
5. 「财团」= 多 Agent 团队分工（对标 TradingAgents 的 bull/bear/analyst）吗？
   答：
6. V1.0 内置几个托管团队/策略？`agent_id` 用户不可选（系统内置）？
   答：

---

### A4. 托管「决策时延 <500ms」与 LLM / 深度引擎矛盾
每笔调仓要产出 `ai_reason`（LLM），深度分析单次要 10–15 分钟，无法都在 500ms 内。
请确认分层设计：

- 托管跑轻量信号（毫秒级）→ 命中才执行，`ai_reason` 走快模型一句话；
- 「查看分析报告」= 点开时才异步生成深度分析；
- 500ms 只指「信号+撮合」路径、不含 LLM？

以上理解对吗？答：__对____（对 / 修改为：______）

---

### A5. 50万初始资金 与「截图同步持仓」的账务规则（全文最大洞）
同步真实券商持仓进来时，这笔持仓的钱从哪来？

- 方案1：视作用 50万 以成本价买入该持仓，现金 = 50万 − Σ(成本×数量)；成本超出 50万 时 → 允许透支 / 拒绝同步 / 红字显示？
- 方案2：同步只导入持仓不扣现金 → total_assets 口径怎么定？
- 方案3：其他 ______

选定：__50w是模拟资产，当用户同步持仓时，这50w自动清空，改用用户持仓和资产____
超出 50万 成本的处理：______

---

### A6. V1.0 有没有「手动下单」？
功能矩阵只有 持仓/托管/量化(V1.1)/自选(V1.1)，没有买卖入口。不托管时用户只能同步持仓+查流水？
「模拟交易」名实要对齐：

- 可选：a) V1.0 无手动下单，只有托管在交易
- 可选：b) 手动买卖走 AI对话自然语言下单（需在 4.6 明确）
- 可选：c) 做独立的下单页（买入/卖出 + 委托确认）
- 可选：d) 其他 ______

选定：__a____

---

## B. 建议补齐（不然开发到一半返工）

### B1. 模拟撮合规则
需定：A股 100股/手、T+1、涨跌停能否成交、佣金/印花税、停牌、成交价取现价还是收盘价、限价/市价、部分成交？

- 撮合价：现价即时成交 / 当日收盘价成交 / 其他 ___现价即时成交___
- 费用：是否计 佣金+印花税？费率？___计算___
- 是否模拟 T+1、涨跌停无法成交？___是___
- 是否要在 PRD 加一节「撮合规则」？______

---

### B2. 自选股 V1.1 才上线，但托管范围含「仅限自选」
V1.0 托管范围是否收敛为「仅限持仓股 / 全市场」？答：__不区分v1.0和v1.1，需求稿中的所有内容本期需要全部实现____

---

### B3. 托管范围选项两处不一致
PRD 4.4 = 持仓/自选 两项；UI 托管设置页 = 持仓/自选/全市场 三项。以哪个为准？
若含「全市场」，标的 universe（数据成本）如何定义？答：___托管设置页 = 持仓/自选/全市场___

---

### B4. 实时行情数据源（钱袋子）
- 行情来源：Wind / 免费A股源（tushare、新浪、腾讯行情）/ 15分钟延迟行情？
  - 建议：A股模拟盘行情走免费源，Wind 仅留「深度分析」层。可否？___行情走wind___
- 托管扫描频率？（分钟级 / 秒级 / 收盘后）→ 决定行情请求量级。___分钟级___

---

### B5. 7.1 非功能指标（1000 QPS / 99.9%）是否模板？
- 按实际内测规模定档（并发几十）是否可以？答：__可以____

---

### B6. AI对话 V1.0 意图清单
「分析我的持仓 / 为什么卖出XX / 今日操作复盘 / 明日策略 / 风险检查 / 深度分析某股」——
V1.0 支持哪几条？（建议列个勾选清单）

- [ ] 持仓诊断/分析我的持仓
- [ ] 托管日志问答（为什么卖出XX）
- [ ] 单标的深度分析（复用 Trade Master？）
- [ ] 明日/今日策略
- [ ] 风险检查
- [ ] 其他 __全部支持____

---

## C. 小修（随稿改即可）

- C1. 通知渠道 V1.0 是否只做 飞书+企微机器人+邮件（个人微信无 API，App 推送要厂商通道）？答：______
- C2. 首页占位入口（量化/自选）：弹「敬请期待」还是给占位页？答：_给占位页_____
- C3. 托管/AI对话范围是否含 港股/美股？（决定红涨绿跌是否换色）答：__包括____
- C4. 「托管收益率」口径：从开启托管那天起算；与手动操作并存时归属谁？答：___归属托管___
- C5. 页面常驻免责声明「模拟数据，仅供学习，不构成投资建议」是否要加？答：___加___

---

## D. 其他开放问题（想到再补）

- D1. `需求稿/原始UI稿/` 里 4 张图，属于本 App 吗？是就作为评审参考一起过。答：___不属于___
- D2. 需要我出一版「托管策略 spec」/「撮合规则」/「账务规则」的草稿章节供你改吗？答：___是的，总结prd文档中需要修改的点___
