# Trade Master · 分析引擎（TradingAgents→A股）· 阶段报告

> 报告日期：2026-09-08
> 项目路径：`product/trade_master/engine/`（vendor 自 TauricResearch/TradingAgents v0.4.0）
> 配套文档：[改造方案](改造方案.md)
> 状态：🟢 P0–P4 全部完成（Wind 数据层 + A股规则 + 中文决策 + FastAPI 服务，完整 propagate 验证通过）

## 一、项目概述

以原版 [TauricResearch/TradingAgents](https://github.com/TauricResearch/TradingAgents)（v0.4.0，全量 Apache 2.0）多智能体金融分析框架为地基，改造为**适配 A股市场**的深度分析引擎，融合进 Trade Master（AlphaPilot）产品体系。数据层 yfinance 全量替换为 **Wind（万得）**，LLM 后端用 **DeepSeek**，输出 A股规则 + 中文决策。

**多智能体流水线**：分析师（市场/情绪/新闻/基本面，ReAct+工具）→ 牛/熊研究员辩论 → 研究经理 → 交易员 → 激进/保守/中性风险辩论 → Portfolio Manager 终裁。

## 二、已完成的选型决策

| 决策点 | 结论 | 理由 |
|---|---|---|
| 地基 | 原版 TauricResearch/TradingAgents v0.4.0 | 全量 Apache 2.0 可商用；结构化输出/Portfolio Manager/checkpoint/决策日志；数据层抽象干净（262 行 interface.py） |
| 与 trade_master 关系 | **融合**（引擎落进 `engine/`） | 复用现有 FastAPI/前端产品体系 |
| A股适配深度 | **数据 + 规则 + 中文全栈** | 见改造方案 |
| 数据源 | **全量 Wind**，移除 yfinance | 用户已有 Wind Key，覆盖 A股/港股/美股/指数/宏观 |
| LLM 后端 | **DeepSeek**（`deepseek-v4-pro` 深思考 + `deepseek-v4-flash` 快思考） | 原版原生支持（`llm_clients` 已内置）；沿用现有配置 |
| 交付形态 | **CLI + FastAPI** | 本阶段先 CLI 跑通，P4 加 FastAPI |

## 三、技术路线（实现细节）

### Wind 数据层接入方式
- Wind 通过 **7 个 MCP 服务**（HTTP JSON-RPC + Bearer 认证）暴露数据，端点 `https://mcp.wind.com.cn/vserver_<type>/mcp/`。
- 自写 Python 客户端 `engine/tradingagents/dataflows/wind.py`：`_wind_rpc`（initialize + tools/call + SSE 解析）→ `_call_tool` → 各 vendor 函数。
- vendor 函数**返回格式对齐原版 yfinance 的字符串约定**（CSV/文本），上层 agent 无感切换。

| 原版 vendor 方法 | Wind 工具 | 说明 |
|---|---|---|
| `get_stock_data`（OHLCV） | `stock_data.get_stock_kline` | TIME/OPEN/MATCH/HIGH/LOW/VOLUME → Date/Open/Close/High/Low/Volume |
| `get_indicators`（技术指标） | 本地 stockstats 计算 | `load_ohlcv` 已路由到 Wind，指标定义与 yfinance 版一致 |
| `get_fundamentals` | `stock_data.get_stock_fundamentals`（NL） | 返回 PE/PB/ROE/毛利率等关键指标 |
| `get_balance_sheet`/`cashflow`/`income` | `stock_data.get_stock_fundamentals`（NL，分报表提问） | 关键科目 |
| `get_news`/`get_global_news` | `financial_docs.get_financial_news` | 中文新闻 |
| `get_insider_transactions` | `stock_data.get_stock_equity_holders`（近似） | 股东数据 |
| `get_macro_indicators` | 暂未映射（可选类目，降级） | Wind EDB 待接入 |
| `get_prediction_markets` | 无 Wind 对应（可选类目，降级） | 原 Polymarket |

### 代码转换层
- `to_wind_code()`：`.SS`→`.SH`（沪）、`.SZ`/`.HK`/`.O` 保留、6 位数字按首位推断交易所。
- 原版 `benchmark_map` 已含 `.SS→000001.SS`（上证综指）、`.SZ→399001.SZ`（深证成指）。
- 原版已有 `output_language` 配置（P3 中文决策只需改配置，内部辩论保持英文保质量）。

## 四、当前进度

- [x] P0 摸底（DeepSeek 配置 / 原版依赖 / Wind 数据验证）
- [x] P1 跑通原版框架 + DeepSeek（init 14.5s、单次 LLM 0.5s 返回 'OK'）
- [x] 框架 vendor 进 `product/trade_master/engine/`（72 个 py 文件）+ editable 重装
- [x] P2 Wind 数据层：`wind.py` + `interface.py` 注册 + `default_config.data_vendors=wind` + `load_ohlcv` 路由
- [x] P2 单元验证：OHLCV(10行)/技术指标(close_50_sma=1258.6)/基本面(PE_TTM 20.2/PB 6.55/ROE 17.95)/中文新闻 全通
- [x] P2 完整 propagate 跑通（600519.SS → **Hold**，718s，全链路 9 节点）
- [x] P3 代码完成：A股规则注入市场/基本面分析师 + `output_language=Chinese` + 中文评级映射（`买入/增持/持有/减持/卖出`）
- [x] P3 中文全量验证（600519.SS → **Hold（持有）**，661.9s，中文输出端到端正常，结构化输出枚举与中文指令无冲突）
- [x] P4 FastAPI 服务：`app/main.py`（`/health` + `/analysis`）+ `app/analysis.py`，uvicorn 启动 `/health` 200 OK
- [x] P5 交付物：改造方案文档、阶段报告、engine/README、进度/问题/待办

## 五、遇到的问题集合（随开发持续更新）

**1. yfinance 被 Yahoo 限流（429 Too Many Requests）**
- 现象：market analyst 调 `get_stock_data` → `yf.Ticker().history()` → `YFRateLimitError`。
- 根因：Yahoo Finance 对国内 IP 限流/不稳定（与 HuggingFace/GitHub 同类网络环境问题）。
- 解决：**全量切换 Wind**（本方案核心动机）。

**2. Wind 配置文件格式误判**
- 现象：`_get_api_key` 报 `VendorNotConfiguredError`。
- 根因：`~/.wind-aifinmarket/config` 是 `WIND_API_KEY=xxx` shell 格式，不是 JSON。
- 解决：`_get_api_key` 兼容 shell（`WIND_API_KEY=`）与 JSON（`{"wind_api_key":}`）两种格式。

**3. Wind MCP 响应被 requests 按 ISO-8859-1 解码，中文多字节被切碎**
- 现象：SSE 解析 `json.loads` 报 `Unterminated string`，且响应被切成 8 行。
- 根因：响应 `Content-Type: text/event-stream` 无 charset，`requests` 默认 ISO-8859-1；中文"元"的 0x85 字节被当成 NEL 换行符（`str.splitlines()` 会拆 U+0085），JSON 被切碎。
- 解决：显式 `resp.content.decode("utf-8")`。

**4. Wind 长区间（5年）K 线日期时区混用**
- 现象：`pd.to_datetime` 报 `ValueError: Mixed timezones detected`。
- 根因：长区间数据部分带 `+08:00`、部分不带。
- 解决：只取日期部分 `df["Date"].astype(str).str[:10]`，忽略时区。

**5. pandas 升级到 3.0.5（观察项）**
- 原版 `pyproject.toml` 声明 `pandas>=2.3.0`，实际装到 3.0.5。当前 Wind/stockstats 路径未踩雷，留作观察。

**6. Wind vendor 函数签名未对齐框架工具调用**
- 现象：情绪分析师调 `get_news.func(ticker, start_date, end_date)` 报 `TypeError: get_news() takes 1 to 2 positional arguments but 3 were given`。
- 根因：框架工具按各自签名调 `route_to_vendor(method, ...)`，而 Wind 版 `get_news`/`get_global_news` 签名写少了参数（yfinance 版是 `(ticker, start_date, end_date)`）。
- 解决：逐一核对 `agents/utils/*_tools.py` 的 `route_to_vendor` 调用，补齐 `get_news(ticker, start_date, end_date)`、`get_global_news(curr_date, look_back_days, limit)` 签名。

**7. 前端打不开/实时分析 401 —— 端口 8000 被别的项目占用**
- 现象：打开 `http://localhost:8000` 看到的是别人的页面（标题"**A股行情对话 Agent**"，非本项目）；点"实时分析"报 `401 Authentication Fails, api key: ****52ba is invalid`。
- 排查：`DEEPSEEK_API_KEY` 与 lingua_mate 标准 key 对 `api.deepseek.com` 的 `/models` 与 `/chat/completions`（模型 `deepseek-v4-pro`）均 **HTTP 200 有效**——key 本身没问题。`lsof -iTCP:8000` 显示端口被 **`ai-tradeing-wjx/.venv/bin/uvicorn backend.app.main:app --reload`**（自 11-26 起长驻）占用。
- 结论：浏览器进的是那个项目的后端，它拿自己失效的 key 调 LLM → 401，与本项目无关。
- 解决：**本项目改用 8010 端口**（`uvicorn app.main:app --port 8010`），`http://localhost:8010` 正常（/health 200、演示报告 12 章节、决策 持有）。如确需 8000，先停掉 `ai-tradeing-wjx`。

## 六、决策记录

1. **选原版非中文版**：全量 Apache 2.0（中文版 `app/`+`frontend/` 专有闭源，商用需单独授权）；原版工程更先进（结构化输出/Portfolio Manager/checkpoint）；数据层抽象干净易插 Wind。
2. **Wind provider 返回字符串对齐 yfinance**：最小改动，上层 agent 无感；避免重写分析师 prompt 的数据契约。
3. **技术指标走本地 stockstats 而非 Wind `get_stock_technicals`**：复用原版指标定义，`load_ohlcv` 单一改动点（路由到 Wind），指标口径不变。
4. **财务三表走 Wind NL 工具（`get_stock_fundamentals`）**：Wind 未提供结构化三表拆分，改用关键科目问题式提问，满足分析师需求。
5. **macro/prediction 暂降级**：均为 OPTIONAL 类目（原版设计为"失败不中断主链路"），先返回 sentinel，后续补 Wind EDB 映射。

## 七、后续待办（TODO）

- [ ] P3：移植 CN 版中国分析师 A股规则（涨跌停/T+1/ST/北向资金）；`output_language="Chinese"`；结构化输出评级枚举中文（买入/增持/持有/减持/卖出）。
- [ ] P3：A股情绪源（中文新闻/雪球/股吧）替代 Reddit/StockTwits（当前 A股情绪分析师无数据）。
- [ ] P4：FastAPI `POST /analysis` 路由 + 保留 CLI。
- [ ] P4：`WIND_API_KEY` 补进 `.env`（当前读 `~/.wind-aifinmarket/config`，部署需显式）。
- [ ] P2 补：Wind EDB 宏观指标映射；美股 `.O` 代码确定交易所（当前裸代码未映射）。

## 八、第二轮完善（2026-09-08 下午）：Wind 全量接口 + 前端 demo + 模拟盘规划

### 8.1 Wind 全量接口审阅与集成
审阅 Wind 7 个 server / **39 个工具**，在 `wind.py` 新增 6 项数据能力（均已单元验证）：

| 新能力 | Wind 工具 | 说明 |
|---|---|---|
| 分钟级 K 线 | `get_stock_kline(period)` | period 1=1分钟…10=日K/11=周K/12=月K；`get_stock_quote` 返回 1 分钟级量价（单日 240 条）|
| 宏观指标 | `economic_data`(EDB) | 实现原 stub，返回 CPI/GDP 等多源时间序列 |
| 公司事件 | `get_stock_events` | 替换 equity_holders 近似，覆盖分红/增减持/ST/解禁 |
| 公司公告 | `get_company_announcements` | 公告原文 |
| 风险指标 | `get_risk_metrics` | Beta/年化波动率/最大回撤/Sharpe/VaR |
| 公司档案 | `get_stock_basicinfo` | 名称/行业/板块（待接入 instrument 解析替代 yfinance）|

**分钟级数据结论（用户任务 2）**：✅ 有。`get_stock_kline` 的 `period` 参数 + `get_stock_quote` 均支持分钟级；`aftype` 支持前/后/不复权。

### 8.2 前端 demo（用户任务 4）
- `static/index.html`：原生 HTML/CSS/JS 单页（marked.js 渲染 markdown）。
- 输入标的 + 日期 → 「演示结果（秒开）」/「实时分析（10-15 分钟）」→ 决策徽章 + 流水线 + 12 段完整报告（可折叠）。
- FastAPI 新增 `GET /`（前端页）、`GET /analysis/demo`（读 `data/sample_report.json`）。
- 演示数据由 `scripts/dump_report.py` 生成（跑一次完整分析存 JSON）。

### 8.3 模拟盘交易规划（用户任务 1，真实接口跳过）
真实券商接口（QMT/PTrade/掘金）暂无，改用**模拟盘（paper trading）**闭环：
- **撮合引擎**：日线收盘价模拟成交（T+1、涨跌停不可成交、手续费/印花税/滑点）。
- **持仓/账户**：现金 + 持仓 + 当日盈亏 + 总资产曲线（SQLite 持久化）。
- **信号→模拟下单**：Portfolio Manager 的 5 档评级 + 交易员目标价 → 映射为模拟买卖指令。
- **复盘**：决策日志 + 已实现收益回填（原版自带 reflect 机制）作为模拟盘的对账单。
- 后续再决定是否接真实券商（用户已有券商接口时）。

### 8.4 遗留待办
- 把 `get_risk_metrics`/`get_company_announcements`/`get_company_name` 接入分析师工具与 prompt（当前仅数据层函数，未绑到 agent）。
- 分钟级数据接入分析链路（当前引擎仍用日线，分钟级已具备取数能力）。
- A股情绪源（雪球/股吧）替代 Reddit/StockTwits。
- 美股裸代码 `.O` 交易所判定。
- `/analysis` 异步化（后台任务 + 轮询）。

### 8.5 第三轮：自然语言对话入口（NL → 意图 → 澄清 → 执行）
用户反馈手填「代码+后缀+日期」太累，改为**单输入框一句话需求**，由大模型拆解成可执行任务，歧义则追问，失败页面上给反馈。已实现并 HTTP 层验证：

- **后端 `app/intent.py`**（新增）+ `POST /chat`：DeepSeek(flash) 把多轮对话拆成
  `{mode, ticker, name, market, date, question}`（`mode=ask` 澄清 / `analyze` 执行），日期只回
  `today/yesterday/具体日期` 标记，**由后端确定性换算**（周末/收盘前回退最近交易日），避免模型算错。
- **Wind 名称→代码解析**：复用 `get_stock_basicinfo`（NL 表格）双向解析；裸 6 位代码**先用本地前缀映射定
  交易所再查**（避免 Wind 默认把 159779 当成沪债「岷水次」，已修）；债券主体（含"债/水务/次级"）自动过滤；
  多市场（A+H）识别 → 追问，A股名自动解析，港股/美股追问后由模型直接带代码。
- **前端 `static/index.html` 重写为对话式**：单输入框 + 多轮气泡；`ask`→追问；`plan`→展示解析结果 +
  「✅ 开始分析 / ↩️ 不是这个」二次确认（避免 15 分钟跑错标的）；执行后渲染决策徽章+流水线+12 段报告。
- **失败页面反馈**：`/analysis` 数据类错误返回 **400 + 中文 detail**（不再裸 500），前端红框展示原因。
- **代码映射升级（wind.py）**：`.SS` 不再一刀切→沪市，按 6 位前缀判定（6/9/5→沪、92/8/4→北交所、其余→深）；
  港股 Wind 代码统一 **5 位补零**（0700.HK→00700.HK）。

**实测链路**（HTTP，deepseek-flash + Wind）：
- 「帮我分析一下今天159779这支基金…」→ plan `159779.SZ 招商中证消费电子主题ETF`
- 「分析中芯国际…」→ ask（列出 A股 688981.SH / 港股 00981.HK）；补答「A股那支」→ plan 688981.SH；「港股」→ plan 00981.HK
- 「分析贵州茅台…」→ plan `600519.SH`

### 8.6 降本：移除新闻分析师的 Wind 宏观取数（`query_economic_indicator_data`）
用户在万得后台调用日志看到该查询消耗大量 token，询问来由。定位：news analyst 在写宏观环境报告时为「用真实数据支撑」而走 ReAct 调 `get_macro_indicators`
→ Wind EDB 的 NL-LLM 服务（`observation=10`、多指标、每次完整分析都会触发、可多轮调用，故 token 大；且原版 prompt 仍引导美国指标，A股场景多为无关消耗）。
**决策（用户选 A）：A股直接去掉。** 已从 news ToolNode 与 news 分析师 tools/prompt 摘除 `get_macro_indicators`（宏观背景由 `get_global_news` 覆盖）。实现层
（wind.py/interface/macro_data_tools/agent_utils re-export）保留，未来可一行恢复。

### 8.7 Wind 余额耗尽误报 + key 轮换 + 错误文案透出
`06862.HK` 实时分析返回 400「no kline rows」——排查发现 Wind 全站（含 A股/新闻端点）原始返回均为 `余额不足，请先充值`：**账号积分耗尽**，与标的无
关。根因两处：
- **key 用尽**：轮换新 Wind key（仅存 `~/.wind-aifinmarket/config`，不入库），A股/港股（`00700.HK` 腾讯，港股 5 位补零链路首次以真实数据验证）/新闻全部恢复。
- **文案掩盖真因**：`_call_tool` 对 Wind 非 JSON 错误文本原降级为 `{"raw":...}`，被上层当成"空数据"吞成 `NoMarketDataError: no kline rows`。
  修复：新增 `VendorRejectedError(VendorError, ValueError)`，`_call_tool` 遇非 JSON 文案直接抛错透出 Wind 原文 → `/analysis` 命中 ValueError→400 可读分支，
  显示「Wind stock_data/get_stock_kline 返回：余额不足，请先充值」而非误导性的"请检查股票代码"。合法空 JSON（真无数据）仍走原 `NoMarketDataError` 语义。

### 8.8 /chat 偶发"解析失败"——意图拆解 LLM 的 max_tokens 截断
实时对话反复提示「解析需求时出了点问题（ValueError）」。定位：`app/intent.py:_llm_json` 用 `deepseek-v4-flash` 拆意图，但 `max_tokens=1200` 太小——
该模型也消耗 hidden `reasoning_tokens`，部分输入"思考"就把 1200 额度占满，可见 `content` 为空串 → `_extract_json("")` 抛 ValueError。思考短时成功、长时空返回，
故**偶发且看起来随机**（同句有时好有时坏）。修复：`max_tokens` 默认 1200 → 8000（与记忆标准"推理模型≥8000"一致），循环 8 次 0 失败验证。
另：服务器无 `--reload`，此前 wind.py/errors.py/intent.py 改动在运行进程里均未生效，已重启（uvicorn app.main:app --port 8010）加载。
