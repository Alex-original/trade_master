# TradingAgents → A股适配 · 改造方案

> 创建日期：2026-09-08
> 目标：以原版 [TauricResearch/TradingAgents](https://github.com/TauricResearch/TradingAgents)（v0.4.0）为地基，改造成适配 A股市场的多智能体金融分析引擎，**融合进 trade_master（AlphaPilot）**，最终支撑「个人可用 + 可商用」的金融 APP。
> 本阶段范围：**跑通原版 + 适配 A股**；量化交易功能暂缓。

---

## 一、选型结论（已定）

| 决策点 | 结论 | 理由 |
|---|---|---|
| 地基 | **原版 TauricResearch/TradingAgents** | 全量 Apache 2.0 可商用；工程更先进（结构化输出/Portfolio Manager/checkpoint/决策日志）；数据层抽象干净 |
| 与 trade_master 关系 | **融合**：引擎落进 trade_master，接 FastAPI | 复用现有产品体系 |
| A股适配深度 | **数据 + 规则 + 中文全栈** | 数据走 Wind；注入涨跌停/T+1/ST/北向资金；中文决策 |
| 数据源 | **全量走 Wind**，移除 yfinance | 用户已有 Wind API Key，覆盖 A股/港股/美股/指数/基金/宏观 |
| 交付形态 | **CLI + FastAPI 服务** | 本阶段 CLI 跑通 + 包一层 HTTP 接口 |
| LLM 后端 | **DeepSeek**（`deepseek-v4-pro` 深思考 + `deepseek-v4-flash` 快思考） | 原版原生支持；沿用现有配置 |

---

## 二、现状摸底结论（P0）

1. **DeepSeek 后端原版已原生支持**：`tradingagents/llm_clients/factory.py` 把 `deepseek` 当 OpenAI 兼容 provider 处理；`model_catalog.py` 已内置 `deepseek-v4-pro`（思考）/ `deepseek-v4-flash`（快）；`api_key_env.py` 读 `DEEPSEEK_API_KEY`。与用户现有模型名一致，**无需改 LLM 客户端**。
2. **数据层抽象干净**：`tradingagents/dataflows/interface.py`（263 行）用 `VENDOR_METHODS`（方法→厂商实现映射）+ `data_vendors`（分类→厂商链）做路由。加 Wind 只需：新增 `wind.py` provider + 注册映射 + 改 `data_vendors`。
3. **原版已有 A股基准映射**：`default_config.py` 的 `benchmark_map` 已含 `.SS→000001.SS`（上证综指）、`.SZ→399001.SZ`（深证成指）、`.HK→^HSI`。
4. **原版已有 `output_language` 配置**（默认 `English`）→ 中文决策只需改配置，不必重写全部 prompt（内部辩论仍英文，仅分析师报告与最终决策转中文）。
5. **Wind 数据已验证**：`get_stock_kline` 能取 A股日线 OHLCV（TIME/OPEN/MATCH=收盘/HIGH/LOW/VOLUME，价格单位元，量单位股），数据新鲜。
6. **原版已有 backtrader 依赖**（`pyproject.toml`），为后续量化回测留了口子（本阶段暂不启用）。

---

## 三、改造点清单

### P1 · 跑通原版（目标：600519.SH 冒烟出完整报告）
- 建 venv，`pip install` 原版依赖（Tsinghua 镜像）。
- 配 `.env`：`DEEPSEEK_API_KEY`；config：`llm_provider=deepseek`、`deep_think_llm=deepseek-v4-pro`、`quick_think_llm=deepseek-v4-flash`、`max_tokens=8000`（推理模型需足额 token 预算）。
- 先以 yfinance 数据源跑通美股/港股冒烟，确认多智能体流水线可运行，再切 A股。

### P2 · Wind 数据层接入（全量替换 yfinance）
- 新增 `tradingagents/dataflows/wind.py`，实现 vendor 函数，**返回格式对齐现有 yfinance 的字符串（CSV/文本）约定**：
  | 原版 vendor 方法 | Wind 工具（server_type.tool） | 字段映射 |
  |---|---|---|
  | `get_stock_data`（OHLCV 日线） | `stock_data.get_stock_kline` / `get_stock_quote` | TIME→Date、OPEN→Open、MATCH→Close、HIGH→High、LOW→Low、VOLUME→Volume |
  | `get_indicators`（技术指标） | `stock_data.get_stock_technicals` | 需对齐原版 `get_stock_stats_indicators_window` 输出 |
  | `get_fundamentals` | `stock_data.get_stock_fundamentals` | 财务概览 |
  | `get_balance_sheet` / `get_cashflow` / `get_income_statement` | `stock_data.get_stock_fundamentals`（按报表拆） | 需确认 Wind 是否拆分三表 |
  | `get_news` / `get_global_news` | `financial_docs.get_financial_news` | 新闻/宏观 |
  | `get_insider_transactions` | `stock_data.get_stock_equity_holders` / `get_stock_events` | 股东/事件（近似） |
  | `get_macro_indicators` | `economic_data.query_economic_indicator_data` | 宏观 EDB |
  | `get_prediction_markets` | 无对应 → 降级（可选类目） | 返回 sentinel |
- 在 `interface.py` 注册：`VENDOR_LIST` 加 `wind`，`VENDOR_METHODS` 各方法加 `"wind": ...`，`default_config.data_vendors` 各分类改为 `wind`。
- Wind 代码映射：A股 `.SH/.SZ`、港股 `.HK`、美股 `.O`（Wind 后缀规范），需写一个 ticker 转换层（yfinance 后缀 `.SS/.SZ` ↔ Wind `.SH/.SZ`）。

### P3 · A股规则 + 中文全栈
- **规则注入**：从中文版移植 `china_market_analyst.py` 的 A股规则知识（涨跌停 ±10%/20%、T+1、ST 风险、北向资金、人民币计价）进原版 market analyst / fundamentals analyst 的 prompt（或新增 China Market Analyst 节点）。
- **中文决策**：`output_language="Chinese"`；`SignalProcessor`/trader/portfolio_manager 的输出字段与「买入/持有/卖出」中文映射；Pydantic 结构化输出 schema 的评级枚举加中文标签（买入/增持/持有/减持/卖出）。
- **A股标的解析**：确保 `600519.SH` 等代码能正确解析公司名、货币（人民币）。

### P4 · 融合进 trade_master + FastAPI
- 把改造后的 `tradingagents/` + `cli/` + `main.py` vendor 到 `product/trade_master/engine/`。
- 新增 FastAPI 路由：`POST /analysis`（入参：ticker/日期/深度）→ 调 `TradingAgentsGraph.propagate()` → 返回结构化决策。
- 保留 CLI 入口（`tradingagents analyze ...`）作为调试/脚本用。

### P5 · 交付物
- `改造方案.md`（本文档）、`阶段报告.md`（实时更新）、进度/问题/待办（阶段报告内「问题集合」+「待办」章节）。

---

## 四、目录结构（融合后）

```
product/trade_master/
  .env.example / .env
  engine/                    # vendor 自原版，改造点所在
    tradingagents/
      dataflows/wind.py      # 新增：Wind provider
      llm_clients/           # 原样（DeepSeek 已支持）
      agents/                # 改造：A股规则 + 中文
      graph/                 # 改造：ticker 转换、中文决策
    cli/ main.py
  app/                       # 现有 trade_master（NL→规则→回测）
    analysis.py              # 新增：深度分析 FastAPI 路由
  docs/ 改造方案.md 阶段报告.md
```

---

## 五、风险与依赖

| 风险 | 影响 | 应对 |
|---|---|---|
| pypi 慢 / 依赖装不上 | P1 阻塞 | Tsinghua 镜像；逐个装并记录 |
| Wind 财务三表未拆分（balance/cashflow/income 合并） | P2 部分方法降级 | 用 `get_stock_fundamentals` 拆字段，缺的表返回 sentinel 并在报告记录 |
| Wind 无 prediction_markets（Polymarket 预测市场） | 可选类目 | 降级 sentinel，不影响主链路 |
| 原版 `output_language` 内部辩论仍英文 | 中文"全栈"的边界 | 按原版设计：内部英文保质量，对外中文；如实记录 |
| yfinance 移除后美股/港股数据依赖 Wind | 覆盖面 | Wind 已覆盖，验证 `.O`/`.HK` 代码 |
| DeepSeek 推理模型 token/超时 | P1 冒烟 | `max_tokens>=8000`；必要时 quick_think 用 `deepseek-v4-flash` |

---

## 六、执行顺序

P0 摸底（✅ 完成）→ P1 跑通原版 → P2 Wind 数据层 → P3 A股+中文 → P4 融合+FastAPI → P5 交付物。每阶段结束更新 `阶段报告.md`。
