# Trade Master（AlphaPilot）· 阶段报告

> 报告日期：2026-09-07
> 项目路径：`product/trade_master/`
> 项目代号：**Trade Master**（内部目录名）；对应需求稿产品名 **AlphaPilot 智能量化决策助手**
> 状态：🟢 立项完成，开发进行中（本报告随开发持续更新）

## 一、项目概述

一款面向 **A 股零售投资者（股龄 1–3 年、无代码能力）** 的无代码量化决策辅助工具。用户用一句话描述交易想法（如「连续三天跌停后今天放量」），系统把它解析成结构化「条件积木」，在 A 股历史数据上做拟真回测（扣除印花税/佣金/滑点）验证胜率，并可挂载「模拟条件单」在盘中定时匹配、信号推送到微信。

**核心闭环**：`自然语言 → 结构化规则（JSON Schema 强约束）→ 历史回测验证 → 条件单挂载与信号推送`

> ⚠️ 定位红线：**不做投资建议、不碰资金、不下真实单**——仅作第三方「算法计算 + 规则提醒」工具，交易由用户在自己的券商 App 完成。

### 需求稿来源（`需求稿/`）
| 文件 | 版本 | 视角 | 要点 |
|---|---|---|---|
| `AlphaPilot_MVP_Product_Requirement_Document.pdf` | PRD v1.0.0 | 商业化完整版 | MySQL/ClickHouse + ECharts + 微信服务号，10 周三大阶段，Free/VIP 付费墙矩阵 |
| `AlphaPilot_PRD_v2.0_Architect_Edition.pdf` | PRD v2.0 | Solo Agent / 架构师评审版 | 单人 + AI Agent 交付瘦身：Streamlit + DeepSeek + AkShare + SQLite + Pandas 回测 + WxPusher，3 周三 Sprint |

本项目实际实现以两版需求**合并取舍**为准，技术栈另行决策（见「三、技术路线」）。

## 二、需求要点提炼

### 2.1 用户旅程
1. **策略构建**：输入自然语言 → NL to UI 生成条件积木 → 可视化微调（怕 AI 幻觉 → 用本地规则拦截 + JSON Schema 强约束，积木可手动改）。
2. **策略验证**：看近 3 年历史收益曲线 vs 沪深 300，看年化/最大回撤/胜率/夏普/交易次数（怕滑点手续费失真 → 引擎强制扣真实交易成本）。
3. **实时监控**：盘中定时快照轮询（默认 15 分钟 + 14:45 收盘临界点），微信收信号提醒（省力；免费额度打码引流付费）。
4. **商业转化**：收到「胜率 78% 信号（股票代码已遮挡）」→ FOMO → 升级 VIP（`Teaser-to-Paid` 是北极星指标）。

### 2.2 四大功能模块（P0）
1. **AI 策略解析（NL to UI）**：本地规则拦截（高频固定逻辑免 LLM）+ LLM 结构化输出 → 策略 JSON（标的范围/时间周期/触发条件列表/仓位）→ 前端「条件积木」。
2. **历史回测引擎**：近 3 年 A 股全市场日线（基准：沪深 300）；强制扣印花税（卖出 0.05%）+ 券商佣金（双边 0.025%、单笔最低 5 元）+ 默认滑点 0.1%；输出资金曲线 + 4 项核心指标；**严禁未来函数**。
3. **模拟盘与信号推送**：条件单挂载 → 盘中快照轮询匹配 → 模拟撮合更新持仓/可用资金/当日盈亏（符合 T+1）→ 微信推送（文案中性化）。
4. **商业化 Demo（P1，本阶段轻量）**：免费额度（日 5 次回测）用尽 → 打码（Teaser）推送 + VIP 付费墙卡片；**不接真实支付 SDK**。

### 2.3 合规红线（必须内建）
- 首次进入必须弹《免责声明与风险提示》并倒计时 3 秒才能勾选。
- 推送严禁「推荐买入 / 买入信号 / 强烈推荐」等投顾词，统一「策略规则触发匹配」「计算匹配度 78%」。
- 不托管资金、不接券商自动交易，仅提醒。

### 2.4 关键验收指标（对齐开发目标）
| 指标 | 目标 |
|---|---|
| 自然语言解析准确率 | ≥ 90%，非法格式自动重试/容错 |
| 生成积木响应时间（LLM 链路） | P95 ≤ 2.0–2.5 s |
| 单策略 3 年日线回测 | ≤ 1.0–5.0 s（向量化） |
| 快照触发到微信推送 | ≤ 5–10 s |
| 盘中并发挂载策略 | 单机先求稳，量级后置 |

## 三、技术路线（决策：沿用 video-note 实战栈）

> 决策日期：2026-09-07。两版 PRD 建议的栈不同（v1.0 商业化、v2.0 Solo-Demo），最终选择**复用 video-note 已验证上线的成熟栈**，为可部署、可演进、可商业化铺路；行情与回测引擎按量化领域需要选型（AkShare + Pandas）。

| 层 | 选型 | 用途 / 理由 |
|---|---|---|
| 后端框架 | **FastAPI + uvicorn** | 复用 video-note 经验，REST API |
| 前端 | **原生 HTML/CSS/JS + ECharts** | 复用 UI 稿/零构建；收益曲线用 ECharts（全市场对比、Hover 买卖点） |
| LLM | **DeepSeek API（JSON Mode / Structured Outputs）** | 复用标准配置；NL→JSON 解析 |
| 行情数据 | **AkShare（东财/新浪源）+ SQLite 本地缓存** | 免费 A 股历史日线；缓存防封禁 |
| 回测/计算 | **Pandas 向量化回测** | 秒级 3 年日线回测 |
| 业务数据库 | **SQLAlchemy + PostgreSQL**（开发期可 SQLite，部署切 PG） | 用户 / 策略 / 订阅 / 任务表，users 隔离 |
| 推送 | WxPusher（个人微信） | 轻量 Webhook 触发，替代复杂 SDK |
| 定时快照 | APScheduler / 后台线程 | 盘中轮询（默认 15 min + 14:45） |

### 技术选型目录
`技术选型/` 目录将随开发落地维护，当前为空，先见本表。

## 四、当前进度（随开发更新）

> **Sprint 0（2026-09-07）**：策略内核已起步，CLI Demo 跑通（零第三方依赖）。

- [x] 需求稿就位（2 份 PRD PDF，`需求稿/`）
- [x] 立项：技术路线决策（沿用 video-note 栈）
- [x] 搭建项目骨架（目录 / `.env.example` / `requirements.txt` / README / .gitignore）
- [x] 策略领域模型 + JSON Schema 协议（`app/strategy.py` / `app/schemas.py`，dataclass 零依赖起步）
- [x] NL→JSON 本地规则解析内核（`app/nlparse/`：双均线交叉 / 突破单均线（含放量修饰）/ MACD 金叉死叉），`scripts/demo_parse.py` 跑通
- [ ] LLM 兜底解析（`parser._parse_with_llm` 已留接口，Sprint 1 接 DeepSeek）
- [ ] 行情数据接入 + 缓存
- [ ] Pandas 向量化回测引擎（扣税/佣金/滑点）
- [ ] 条件积木前端 + ECharts 回测曲线
- [ ] 条件单模拟撮合 + 账户盈亏（T+1）
- [ ] 盘中快照轮询 + WxPusher 推送
- [ ] 免责声明阻断流 + 文案中性化
- [ ] 免费额度 / 打码（Teaser）商业化 Demo

## 五、Sprint 计划（对齐 PRD v2.0 的 3 周节奏）

| Sprint | 周期 | 交付物 |
|---|---|---|
| **Sprint 1 内核闭环** | 本周起 | 策略模型 + NL 解析 + AkShare 数据 + 向量化回测，先出 **CLI 闭环**（文本→JSON→回测指标） |
| **Sprint 2 Web 化** | +1 周 | FastAPI + 原生前端：条件积木编辑器 + ECharts 曲线；条件单模拟撮合 + 推送 |
| **Sprint 3 上线 Demo** | +1 周 | 免责声明阻断 + 免费额度/打码 Demo + 部署上线 |

## 六、决策记录

1. **技术栈**：沿用 video-note 实战栈（FastAPI + 原生前端 + SQLAlchemy + DeepSeek），替代两版 PRD 建议的 Streamlit 与 MySQL 栈——前者复用已验证工程经验，后者与前端还原度/可部署性取舍一致（video-note 阶段三已验证 Gradio/轻栈还原度不足的教训）。
2. **行情数据**：AkShare + 本地 SQLite 缓存（对齐 PRD v2.0 的成本控制：免自建行情库、防 API 封禁），商业级 MySQL/ClickHouse 行情库后置。
3. **MVP 数据范围**：近 3 年 A 股日线，基准沪深 300；回测时间序列严格撮合、严禁未来函数。
4. **本阶段不接真实支付**：付费墙仅 Demo（打码 + 升级卡片），支付/合规商业化参照 video-note 路线后置。
5. **文案红线**：推送与提示统一中性化文案，内建免责声明阻断流（进策略/回测页前强制勾选）。
6. **indicator 列表达式**：指标表达为「静态列 + 带周期派生列」（`close_price` / `ma20` / `volume_ma5`），以支持「ma5 上穿 ma20」这类双序列穿越，直接对接回测引擎指标计算。JSON Schema 用宽松 pattern（LLM 粗约束）+ `StrategyConfig.from_dict` 严格校验（后端护栏）双层防幻觉。
7. **卖出语义**：支持「显式 sell_conditions」或「holding_days 固定持有 N 日」两种表达；精确撮合语义（触发平仓 vs 到期平仓）在回测 Sprint 定稿。
8. **放量修饰的 MVP 定义**：放量 = 「成交量 > 5 日均量」（默认 1 倍），note 中如实说明；精确倍数（如 1.5×）后置精化。

## 七、遇到的问题集合

**1. 突破均线文案出现「年线线 / 月线线」叠字**
- 现象：`突破年线` 输出成 `突破年线线`，`突破20日均线` 被显示成 `突破月线线`。
- 根因：均线别名映射（年线/半年线/季线/月线 → 周期）后再反向换算，别名本身含「线」字导致拼接重复；且把数字周期（20）翻译成别名（月线）偏离用户原话。
- 解决：`_ma_label` 用匹配到的原始表达原样回显——输入「年线」显示「年线」，输入「MA60/60日线」显示「60日均线」，不再做双向别名换算。

## 八、后续可优化方向

- 分钟级数据与近 10 年回测（VIP 算力档）。
- 用户系统 / 配额计数 / 多策略并发挂载。
- 微信服务号 / 小程序端（当前 WxPusher 个人推送）。
- 与 video-note 类似的多用户商业化（PostgreSQL 用户体系、计费、短信）。
